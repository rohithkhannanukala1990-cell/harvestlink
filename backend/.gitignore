# Backend-local ignores — complements the monorepo root .gitignore.
# Excludes env secrets, build output, and Prisma-generated artifacts.

node_modules/
dist/
.env
*.log

# Timestamped pg_dump outputs from `npm run db:backup`
backups/
